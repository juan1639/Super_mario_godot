extends Node2D

#func _ready():
	#var stylebox = StyleBoxFlat.new()
	#stylebox.bg_color = Color(0, 0, 0, 0.5)  # Negro semi-transparente
	#stylebox.border_width_all = 2
	#stylebox.border_color = Color(1, 1, 1)  # Blanco
#
	#$Panel.add_theme_stylebox("panel", stylebox)

#func _ready():
	#var font = DynamicFont.new()
	#font.font_data = load("res://fuentes/MiFuente.ttf")  # Asegúrate de tener esta fuente
	#font.size = 32  # Tamaño deseado
#
	#$Panel/ScoreNum.add_theme_font("font", font)
	#$Panel/ScoreNum.add_theme_color("font_color", Color(1, 0.8, 0.2))  # Dorado
